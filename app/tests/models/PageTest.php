<?php
 
use Zizaco\FactoryMuff\Facade\FactoryMuff;
 
class PageTest extends TestCase {

    public function test_relation_with_user() {
        //$page = FactoryMuff::create('Page');

        //$this->assertEquals($page->user_id, $page->user()->first()->id());
    }

    public function test_created_at() {
        //$post = FactoryMuff::create('Page');

        //$expected = '/\d{2}\/\d{2}\/\d{4}/';
        //$matches = (preg_match($expected, $post->postedAt())) ? true : false;

        //$this->assertTrue($matches);
    }
}
